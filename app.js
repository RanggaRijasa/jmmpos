const items = [
  {
    id: "cut",
    type: "service",
    label: "Jasa",
    name: "Gunting Rambut",
    price: 160000,
    qty: 0,
  },
  {
    id: "colour",
    type: "service",
    label: "Jasa",
    name: "Hair Colour",
    price: 450000,
    qty: 0,
  },
  {
    id: "cream",
    type: "service",
    label: "Jasa",
    name: "Creambath",
    price: 230000,
    qty: 0,
  },
  {
    id: "keratin",
    type: "service",
    label: "Jasa",
    name: "Keratin Treatment",
    price: 750000,
    qty: 0,
  },
  {
    id: "blow",
    type: "service",
    label: "Jasa",
    name: "Blow Dry",
    price: 120000,
    qty: 0,
  },
  {
    id: "smoothing",
    type: "service",
    label: "Jasa",
    name: "Smoothing",
    price: 650000,
    qty: 0,
  },
  {
    id: "hairspa",
    type: "service",
    label: "Jasa",
    name: "Hair Spa",
    price: 280000,
    qty: 0,
  },
  {
    id: "toning",
    type: "service",
    label: "Jasa",
    name: "Hair Toning",
    price: 320000,
    qty: 0,
  },
  {
    id: "perm",
    type: "service",
    label: "Jasa",
    name: "Digital Perm",
    price: 850000,
    qty: 0,
  },
  {
    id: "manicure",
    type: "service",
    label: "Jasa",
    name: "Manicure",
    price: 110000,
    qty: 0,
  },
  {
    id: "pedicure",
    type: "service",
    label: "Jasa",
    name: "Pedicure",
    price: 135000,
    qty: 0,
  },
  {
    id: "makeup",
    type: "service",
    label: "Jasa",
    name: "Make Up",
    price: 500000,
    qty: 0,
  },
  {
    id: "hairwash",
    type: "service",
    label: "Jasa",
    name: "Hair Wash",
    price: 60000,
    qty: 0,
  },
  {
    id: "blowcatok",
    type: "service",
    label: "Jasa",
    name: "Blow Catok",
    price: 180000,
    qty: 0,
  },
  {
    id: "highlight",
    type: "service",
    label: "Jasa",
    name: "Highlight",
    price: 520000,
    qty: 0,
  },
  {
    id: "rebonding",
    type: "service",
    label: "Jasa",
    name: "Rebonding",
    price: 700000,
    qty: 0,
  },
  {
    id: "hairmask-service",
    type: "service",
    label: "Jasa",
    name: "Hair Mask",
    price: 210000,
    qty: 0,
  },
  {
    id: "totok",
    type: "service",
    label: "Jasa",
    name: "Totok Wajah",
    price: 175000,
    qty: 0,
  },
  {
    id: "kids-cut",
    type: "service",
    label: "Jasa",
    name: "Kids Hair Cut",
    price: 95000,
    qty: 0,
  },
  {
    id: "hair-extension",
    type: "service",
    label: "Jasa",
    name: "Hair Extension",
    price: 1200000,
    qty: 0,
  },
  {
    id: "shampoo",
    type: "product",
    label: "Produk",
    name: "Shampoo Keratin",
    price: 95000,
    qty: 0,
  },
  {
    id: "vitamin",
    type: "product",
    label: "Produk",
    name: "Vitamin Rambut",
    price: 80000,
    qty: 0,
  },
  {
    id: "tonic",
    type: "product",
    label: "Produk",
    name: "Hair Tonic",
    price: 125000,
    qty: 0,
  },
  {
    id: "conditioner",
    type: "product",
    label: "Produk",
    name: "Conditioner Silk",
    price: 90000,
    qty: 0,
  },
  {
    id: "mask",
    type: "product",
    label: "Produk",
    name: "Hair Mask Aloe",
    price: 150000,
    qty: 0,
  },
  {
    id: "serum",
    type: "product",
    label: "Produk",
    name: "Serum Anti Frizz",
    price: 175000,
    qty: 0,
  },
  {
    id: "wax",
    type: "product",
    label: "Produk",
    name: "Styling Wax",
    price: 70000,
    qty: 0,
  },
  {
    id: "spray",
    type: "product",
    label: "Produk",
    name: "Hair Spray",
    price: 85000,
    qty: 0,
  },
  {
    id: "mousse",
    type: "product",
    label: "Produk",
    name: "Volume Mousse",
    price: 99000,
    qty: 0,
  },
  {
    id: "argan",
    type: "product",
    label: "Produk",
    name: "Argan Oil",
    price: 185000,
    qty: 0,
  },
  {
    id: "dry-shampoo",
    type: "product",
    label: "Produk",
    name: "Dry Shampoo",
    price: 115000,
    qty: 0,
  },
  {
    id: "heat-protect",
    type: "product",
    label: "Produk",
    name: "Heat Protector",
    price: 145000,
    qty: 0,
  },
  {
    id: "colour-shampoo",
    type: "product",
    label: "Produk",
    name: "Colour Shampoo",
    price: 135000,
    qty: 0,
  },
  {
    id: "scalp-scrub",
    type: "product",
    label: "Produk",
    name: "Scalp Scrub",
    price: 155000,
    qty: 0,
  },
  {
    id: "leave-in",
    type: "product",
    label: "Produk",
    name: "Leave-in Cream",
    price: 120000,
    qty: 0,
  },
  {
    id: "curl-cream",
    type: "product",
    label: "Produk",
    name: "Curl Cream",
    price: 140000,
    qty: 0,
  },
  {
    id: "repair-ampoule",
    type: "product",
    label: "Produk",
    name: "Repair Ampoule",
    price: 210000,
    qty: 0,
  },
  {
    id: "shine-mist",
    type: "product",
    label: "Produk",
    name: "Shine Mist",
    price: 98000,
    qty: 0,
  },
  {
    id: "root-lift",
    type: "product",
    label: "Produk",
    name: "Root Lift Spray",
    price: 125000,
    qty: 0,
  },
  {
    id: "hair-clip",
    type: "product",
    label: "Produk",
    name: "Hair Clip Set",
    price: 55000,
    qty: 0,
  },
];

const formatter = new Intl.NumberFormat("id-ID", {
  style: "currency",
  currency: "IDR",
  maximumFractionDigits: 0,
});

const customers = [
  {
    id: "dewi",
    code: "CUST.0001",
    name: "Dewi Anggraini",
    phone: "0812 3456 7890",
    status: "Member Gold",
    type: "gold",
    totalVisits: 18,
    lastVisit: "27 Jun 2026",
    reminderDate: "04 Jul 2026",
    dp: 0,
    reward: { serviceId: "cut", serviceName: "Gunting Rambut", progress: 9, target: 10, freeAvailable: 0 },
  },
  {
    id: "siti",
    code: "CUST.0002",
    name: "Siti Rahma",
    phone: "0813 2222 8899",
    status: "Member",
    type: "member",
    totalVisits: 12,
    lastVisit: "20 Jun 2026",
    reminderDate: "27 Jun 2026",
    dp: 0,
    reward: { serviceId: "cream", serviceName: "Creambath", progress: 6, target: 10, freeAvailable: 0 },
  },
  {
    id: "priscila",
    code: "CUST.0003",
    name: "Priscila Tan",
    phone: "0821 9900 1111",
    status: "Non Member",
    type: "non-member",
    totalVisits: 3,
    lastVisit: "13 Jun 2026",
    reminderDate: "20 Jun 2026",
    dp: 0,
    reward: null,
  },
  {
    id: "nina",
    code: "CUST.0004",
    name: "Nina Septiani",
    phone: "0852 5555 4477",
    status: "Member Platinum",
    type: "platinum",
    totalVisits: 24,
    lastVisit: "18 Jun 2026",
    reminderDate: "25 Jun 2026",
    dp: 150000,
    reward: { serviceId: "cut", serviceName: "Gunting Rambut", progress: 10, target: 10, freeAvailable: 1 },
  },
  {
    id: "rika",
    code: "CUST.0005",
    name: "Rika Amelia",
    phone: "0819 7733 4466",
    status: "Member Silver",
    type: "silver",
    totalVisits: 10,
    lastVisit: "17 Jun 2026",
    reminderDate: "24 Jun 2026",
    dp: 100000,
    reward: { serviceId: "hairwash", serviceName: "Hair Wash", progress: 10, target: 10, freeAvailable: 1 },
  },
  {
    id: "maya",
    code: "CUST.0006",
    name: "Maya Putri",
    phone: "0877 4040 7007",
    status: "Member Gold",
    type: "gold",
    totalVisits: 16,
    lastVisit: "15 Jun 2026",
    reminderDate: "22 Jun 2026",
    dp: 50000,
    reward: { serviceId: "keratin", serviceName: "Keratin Treatment", progress: 8, target: 10, freeAvailable: 0 },
  },
  {
    id: "lia",
    code: "CUST.0007",
    name: "Lia Kartika",
    phone: "0896 1188 0099",
    status: "Non Member",
    type: "non-member",
    totalVisits: 1,
    lastVisit: "09 Jun 2026",
    reminderDate: "16 Jun 2026",
    dp: 0,
    reward: null,
  },
  {
    id: "ayu",
    code: "CUST.0008",
    name: "Ayu Lestari",
    phone: "0812 7788 4411",
    status: "Member Silver",
    type: "silver",
    totalVisits: 8,
    lastVisit: "08 Jun 2026",
    reminderDate: "15 Jun 2026",
    dp: 0,
    reward: { serviceId: "cream", serviceName: "Creambath", progress: 8, target: 10, freeAvailable: 0 },
  },
  {
    id: "intan",
    code: "CUST.0009",
    name: "Intan Permata",
    phone: "0822 6060 9911",
    status: "Member Gold",
    type: "gold",
    totalVisits: 20,
    lastVisit: "07 Jun 2026",
    reminderDate: "14 Jun 2026",
    dp: 0,
    reward: { serviceId: "colour", serviceName: "Hair Colour", progress: 10, target: 10, freeAvailable: 1 },
  },
  {
    id: "melati",
    code: "CUST.0010",
    name: "Melati Sari",
    phone: "0856 2000 4433",
    status: "Non Member",
    type: "non-member",
    totalVisits: 2,
    lastVisit: "05 Jun 2026",
    reminderDate: "12 Jun 2026",
    dp: 0,
    reward: null,
  },
  {
    id: "citra",
    code: "CUST.0011",
    name: "Citra Wulandari",
    phone: "0813 9000 7711",
    status: "Member Platinum",
    type: "platinum",
    totalVisits: 31,
    lastVisit: "02 Jun 2026",
    reminderDate: "09 Jun 2026",
    dp: 200000,
    reward: { serviceId: "smoothing", serviceName: "Smoothing", progress: 7, target: 10, freeAvailable: 0 },
  },
  {
    id: "salsa",
    code: "CUST.0012",
    name: "Salsa Nadia",
    phone: "0878 3311 2200",
    status: "Member",
    type: "member",
    totalVisits: 5,
    lastVisit: "01 Jun 2026",
    reminderDate: "08 Jun 2026",
    dp: 0,
    reward: { serviceId: "hairspa", serviceName: "Hair Spa", progress: 5, target: 10, freeAvailable: 0 },
  },
];

const staffOptions = [
  "Kartini",
  "Wira",
  "Siska",
  "Priscila",
  "Sapriti",
  "Nadya",
  "Dewi",
  "Rani",
  "Yuni",
  "Laras",
  "Tika",
  "Mira",
];

const customerHistories = {
  dewi: [
    ["27 Jun 2026", "Hair Colour + Blow", "Kartini", 450000],
    ["20 Jun 2026", "Creambath", "Wira", 230000],
    ["13 Jun 2026", "Gunting Rambut", "Siska", 160000],
    ["06 Jun 2026", "Keratin Treatment", "Priscila", 750000],
    ["30 Mei 2026", "Gunting Rambut", "Kartini", 160000],
    ["22 Mei 2026", "Hair Spa", "Nadya", 280000],
    ["15 Mei 2026", "Hair Wash", "Wira", 60000],
    ["08 Mei 2026", "Blow Dry", "Kartini", 120000],
    ["01 Mei 2026", "Manicure", "Priscila", 110000],
    ["24 Apr 2026", "Creambath", "Wira", 230000],
    ["17 Apr 2026", "Gunting Rambut", "Siska", 160000],
    ["10 Apr 2026", "Hair Colour", "Kartini", 450000],
  ],
  siti: [
    ["20 Jun 2026", "Creambath", "Wira", 230000],
    ["12 Jun 2026", "Hair Spa", "Nadya", 280000],
    ["05 Jun 2026", "Gunting Rambut", "Siska", 160000],
    ["29 Mei 2026", "Hair Wash", "Kartini", 60000],
    ["22 Mei 2026", "Blow Catok", "Priscila", 180000],
    ["15 Mei 2026", "Creambath", "Wira", 230000],
    ["08 Mei 2026", "Hair Mask", "Nadya", 210000],
    ["01 Mei 2026", "Gunting Rambut", "Siska", 160000],
  ],
  priscila: [
    ["13 Jun 2026", "Hair Wash", "Priscila", 60000],
    ["02 Jun 2026", "Blow Dry", "Kartini", 120000],
    ["18 Mei 2026", "Manicure", "Nadya", 110000],
  ],
  nina: [
    ["18 Jun 2026", "Gunting Rambut", "Wira", 160000],
    ["11 Jun 2026", "Keratin Treatment", "Priscila", 750000],
    ["04 Jun 2026", "Hair Colour", "Kartini", 450000],
    ["28 Mei 2026", "Gunting Rambut", "Siska", 160000],
    ["21 Mei 2026", "Hair Spa", "Wira", 280000],
    ["14 Mei 2026", "Gunting Rambut", "Kartini", 160000],
    ["07 Mei 2026", "Creambath", "Nadya", 230000],
    ["30 Apr 2026", "Gunting Rambut", "Wira", 160000],
    ["23 Apr 2026", "Hair Wash", "Sapriti", 60000],
    ["16 Apr 2026", "Gunting Rambut", "Kartini", 160000],
    ["09 Apr 2026", "Blow Dry", "Priscila", 120000],
    ["02 Apr 2026", "Gunting Rambut", "Siska", 160000],
  ],
  rika: [
    ["17 Jun 2026", "Hair Wash", "Nadya", 60000],
    ["10 Jun 2026", "Smoothing", "Sapriti", 650000],
    ["03 Jun 2026", "Gunting Rambut", "Kartini", 160000],
    ["27 Mei 2026", "Hair Wash", "Wira", 60000],
    ["20 Mei 2026", "Creambath", "Siska", 230000],
    ["13 Mei 2026", "Hair Wash", "Nadya", 60000],
  ],
  maya: [
    ["15 Jun 2026", "Keratin Treatment", "Priscila", 750000],
    ["08 Jun 2026", "Hair Colour", "Kartini", 450000],
    ["01 Jun 2026", "Hair Spa", "Wira", 280000],
    ["25 Mei 2026", "Gunting Rambut", "Siska", 160000],
    ["18 Mei 2026", "Keratin Treatment", "Priscila", 750000],
    ["11 Mei 2026", "Creambath", "Wira", 230000],
    ["04 Mei 2026", "Hair Wash", "Kartini", 60000],
  ],
  lia: [
    ["09 Jun 2026", "Hair Wash", "Kartini", 60000],
  ],
};

let activeFilter = "service";
let selectedCustomer = null;
let selectedPayment = "QRIS";
let activeStaffMenu = null;
let searchTerm = "";
let customerSearchTerm = "";
let activeDetailCustomerId = "dewi";
let activeConfirmMode = "payment";

function formatMoney(value) {
  return formatter.format(value).replace(/\u00a0/g, " ");
}

function getMaxQty(item) {
  return item.type === "service" ? 2 : 99;
}

function increaseItem(item) {
  const maxQty = getMaxQty(item);
  if (item.qty >= maxQty) {
    showToast(item.type === "service" ? "Maksimal 2 untuk tiap jasa" : "Qty sudah maksimal");
    return false;
  }

  item.qty += 1;
  return true;
}

function getCustomerBadge(customer) {
  if (!customer) return `<span class="badge muted">Kosong</span>`;
  if (customer.type === "non-member") return `<span class="badge muted">Non Member</span>`;
  return `
    <span class="badge member ${customer.type}">
      <span class="member-mark" aria-hidden="true">M</span>
      <span>${customer.status}</span>
    </span>
  `;
}

function getRewardText(customer) {
  if (!customer?.reward) return "Benefit member belum aktif";
  const reward = customer.reward;
  if (reward.freeAvailable > 0) return `${reward.serviceName}: gratis 1 jasa siap dipakai`;
  return `${reward.serviceName}: ${reward.progress}/${reward.target} menuju gratis 1 jasa`;
}

function renderRewardMeter(customer, mode = "normal") {
  if (!customer?.reward) {
    return `
      <div class="reward-meter ${mode} muted">
        <span>Benefit Member</span>
        <strong>Non-member</strong>
        <small>Benefit buy 10 get 1 belum aktif.</small>
      </div>
    `;
  }

  const reward = customer.reward;
  const active = Math.min(reward.progress, reward.target);
  const dots = Array.from({ length: reward.target }, (_, index) => {
    const done = index < active;
    const free = reward.freeAvailable > 0 && index === reward.target - 1;
    return `<i class="${done ? "done" : ""} ${free ? "free" : ""}"></i>`;
  }).join("");
  const headline = reward.freeAvailable > 0 ? "Gratis 1 jasa siap" : `${active}/${reward.target} kunjungan`;

  return `
    <div class="reward-meter ${mode} ${reward.freeAvailable > 0 ? "ready" : ""}">
      <span>Benefit Member</span>
      <strong>Buy 10 jasa, gratis 1 jasa</strong>
      <small>${reward.serviceName} · ${headline}</small>
      <div class="reward-dots" aria-label="${reward.serviceName} ${active} dari ${reward.target}">${dots}</div>
    </div>
  `;
}

function getAppliedReward(selected) {
  const reward = selectedCustomer?.reward;
  if (!reward || reward.freeAvailable < 1) return null;

  const rewardItem = selected.find((item) => item.id === reward.serviceId && item.type === "service" && item.qty > 0);
  if (!rewardItem) return null;

  return {
    label: `${reward.serviceName} gratis member`,
    serviceName: reward.serviceName,
    amount: rewardItem.price,
    itemId: rewardItem.id,
  };
}

function calculateTotals() {
  const selected = items.filter((item) => item.qty > 0);
  const subtotal = selected.reduce((sum, item) => sum + item.price * item.qty, 0);
  const reward = getAppliedReward(selected);
  const rewardAmount = reward?.amount || 0;
  const dp = selectedCustomer?.dp || 0;
  const payable = Math.max(0, subtotal - rewardAmount - dp);

  return { selected, subtotal, reward, rewardAmount, dp, payable };
}

function visibleItems() {
  const filtered =
    activeFilter === "service" ? items.filter((item) => item.type === "service") : items.filter((item) => item.type === activeFilter);

  if (!searchTerm) return filtered;

  return filtered.filter((item) => item.name.toLowerCase().includes(searchTerm));
}

function updateSearchPlaceholder() {
  const input = document.querySelector("#item-search");
  input.placeholder = activeFilter === "service" ? "Cari jasa..." : "Cari produk...";
}

function renderCustomer() {
  const summary = document.querySelector("#customer-summary");
  const status = document.querySelector("#customer-status");
  const toggle = document.querySelector("#customer-toggle");

  if (!selectedCustomer) {
    summary.classList.add("empty");
    summary.innerHTML = `
      <span>Pelanggan</span>
      <strong>Belum dipilih</strong>
      <small>Pilih pelanggan untuk transaksi ini</small>
    `;
    status.outerHTML = `<span class="badge muted" id="customer-status">Kosong</span>`;
    toggle.textContent = "Pilih";
    return;
  }

  summary.classList.remove("empty");
  summary.innerHTML = `
    <span>Pelanggan</span>
    <strong>${selectedCustomer.name}</strong>
    <small>${selectedCustomer.phone}</small>
    ${renderRewardMeter(selectedCustomer, "strip")}
  `;
  status.outerHTML = getCustomerBadge(selectedCustomer).replace("class=\"badge", "id=\"customer-status\" class=\"badge");
  toggle.textContent = "Ganti";
}

function renderCustomerDropdown() {
  const dropdown = document.querySelector("#customer-dropdown");
  dropdown.innerHTML = customers
    .map((customer) => {
      const activeClass = selectedCustomer?.id === customer.id ? " active" : "";
      return `
        <button class="customer-option${activeClass}" type="button" data-customer="${customer.id}">
          <span>
            <strong>${customer.name}</strong>
            <small>${customer.phone}</small>
          </span>
          ${
            customer.type === "non-member"
              ? `<em class="muted">Non Member</em>`
              : `<em class="member"><span aria-hidden="true">M</span>${customer.status}</em>`
          }
        </button>
      `;
    })
    .join("");
}

function renderCustomerList() {
  const list = document.querySelector("#customer-list");
  if (!list) return;

  const filtered = customers.filter((customer) => {
    const keyword = `${customer.code} ${customer.name} ${customer.phone} ${customer.status}`.toLowerCase();
    return keyword.includes(customerSearchTerm);
  });

  if (!filtered.length) {
    list.innerHTML = `
      <div class="customer-empty">
        <strong>Tidak ada pelanggan</strong>
        <span>Coba cari nama atau nomor HP lain.</span>
      </div>
    `;
    return;
  }

  list.innerHTML = filtered
    .map(
      (customer) => `
        <article class="customer-row">
          <span class="customer-code">${customer.code}</span>
          <div class="customer-cell-main">
            <strong>${customer.name}</strong>
            <small>${customer.phone}</small>
          </div>
          <div>${getCustomerBadge(customer)}</div>
          <div>${renderRewardMeter(customer, "compact")}</div>
          <span class="last-visit">${customer.lastVisit}</span>
          <button class="table-action" type="button" data-detail-customer="${customer.id}">Lihat Detail</button>
        </article>
      `,
    )
    .join("");
}

function renderCustomerDetail(customerId = activeDetailCustomerId) {
  const customer = customers.find((entry) => entry.id === customerId) || customers[0];
  activeDetailCustomerId = customer.id;
  const fallbackService = customer.reward?.serviceName || "Hair Wash";
  const fallbackHistory = [
    [customer.lastVisit, fallbackService, "Kartini", items.find((item) => item.id === customer.reward?.serviceId)?.price || 160000],
    ["25 Mei 2026", "Creambath", "Wira", 230000],
    ["18 Mei 2026", "Gunting Rambut", "Siska", 160000],
    ["11 Mei 2026", "Hair Wash", "Nadya", 60000],
    ["04 Mei 2026", "Blow Dry", "Priscila", 120000],
    ["27 Apr 2026", "Hair Spa", "Wira", 280000],
  ];

  document.querySelector("#detail-name").textContent = customer.name;
  document.querySelector("#detail-badge").innerHTML = getCustomerBadge(customer);
  document.querySelector("#detail-phone").textContent = `No HP: ${customer.phone}`;
  document.querySelector("#detail-visits").textContent = `${customer.totalVisits} kali`;
  document.querySelector("#detail-reward").innerHTML = renderRewardMeter(customer, "detail");
  document.querySelector("#detail-reminder").textContent = `Hubungi 7 hari setelah jasa terakhir: ${customer.reminderDate}.`;

  const history = customerHistories[customer.id] || fallbackHistory;
  document.querySelector("#detail-history-list").innerHTML = history
    .map(
      ([date, service, staff, amount]) => `
        <article class="history-row">
          <strong>${date}</strong>
          <span>${service} · ${staff}</span>
          <b>${formatMoney(amount)}</b>
          <em>Selesai</em>
        </article>
      `,
    )
    .join("");
}

function renderItems() {
  const grid = document.querySelector("#item-grid");
  const entries = visibleItems();

  if (!entries.length) {
    grid.innerHTML = `
      <div class="empty-items">
        <strong>Tidak ada hasil</strong>
        <span>Coba kata kunci lain.</span>
      </div>
    `;
    return;
  }

  grid.innerHTML = entries
    .map((item) => {
      return `
        <article class="item-card" data-id="${item.id}">
          <h3>${item.name}</h3>
          <div class="item-bottom">
            <span class="price">${formatMoney(item.price)}</span>
          </div>
        </article>
      `;
    })
    .join("");
}

function renderCart() {
  const { selected, reward, rewardAmount, dp, payable } = calculateTotals();
  const list = document.querySelector("#cart-list");

  if (!selected.length) {
    list.innerHTML = `
      <div class="empty-cart">
        <strong>Keranjang kosong</strong>
        <span>Tap jasa atau produk untuk mulai transaksi.</span>
      </div>
    `;
  } else {
    list.innerHTML = selected
      .map((item) => {
        const staffMenu =
          activeStaffMenu === item.id
            ? `
              <div class="staff-menu">
                ${staffOptions
                  .map(
                    (staff) =>
                      `<button type="button" data-staff="${staff}" data-id="${item.id}">
                        <span>${staff}</span>
                        ${item.staff === staff ? `<b>Dipilih</b>` : ""}
                      </button>`,
                  )
                  .join("")}
              </div>
            `
            : "";
        const detail =
          item.label === "Jasa"
            ? `
              <button class="staff-select" type="button" data-staff-for="${item.id}">
                ${item.staff || "Pilih petugas"}
              </button>
              ${reward?.itemId === item.id ? `<span class="reward-note">Gratis member aktif</span>` : ""}
              ${staffMenu}
            `
            : `<span class="cart-note">Quantity produk</span>`;
        return `
          <article class="cart-row">
            <small>${item.label}</small>
            <div class="cart-row-top">
              <strong>${item.name}</strong>
              <b>${formatMoney(item.price * item.qty)}</b>
            </div>
            <div class="cart-row-bottom">
              ${detail}
              <div class="cart-qty" data-id="${item.id}">
                <button type="button" data-delta="-1">−</button>
                <strong>${item.qty}</strong>
                <button type="button" data-delta="1" ${item.qty >= getMaxQty(item) ? "disabled" : ""}>+</button>
              </div>
            </div>
          </article>
        `;
      })
      .join("");
  }

  const rewardLine = document.querySelector("#reward-line");
  const dpLine = document.querySelector("#dp-line");
  rewardLine.hidden = rewardAmount === 0;
  dpLine.hidden = dp === 0;
  document.querySelector("#reward").textContent = `- ${formatMoney(rewardAmount)}`;
  document.querySelector("#reward-label").textContent = reward ? `Gratis Member: ${reward.serviceName}` : "Gratis Member";
  document.querySelector("#dp").textContent = `- ${formatMoney(dp)}`;
  document.querySelector("#total").textContent = formatMoney(payable);
  document.querySelector(".cart-head .badge").textContent = `${selected.length} item`;
}

function setPayment(method) {
  selectedPayment = method;
  document.querySelectorAll("[data-payment]").forEach((button) => {
    button.classList.toggle("active", button.dataset.payment === selectedPayment);
  });
}

function openConfirmation(mode) {
  activeConfirmMode = mode;
  const { selected, reward, rewardAmount, dp, payable } = calculateTotals();
  const modal = document.querySelector("#confirm-modal");
  const title = document.querySelector("#modal-title");
  const copy = document.querySelector("#confirm-copy");
  const summary = document.querySelector("#confirm-summary");
  const confirmButton = document.querySelector("#confirm-payment");
  const customerLabel = selectedCustomer
    ? `${selectedCustomer.name} · ${selectedCustomer.status}`
    : "Pelanggan belum dipilih";

  title.textContent = mode === "draft" ? "Simpan Draft Transaksi" : "Konfirmasi Pembayaran";
  copy.textContent =
    mode === "draft"
      ? "Transaksi akan masuk daftar pending dan bisa dilanjutkan nanti."
      : "Pastikan metode pembayaran dan total sudah sesuai.";
  confirmButton.textContent = mode === "draft" ? "Simpan Draft" : "Bayar Sekarang";

  summary.innerHTML = `
    <div><span>Pelanggan</span><strong>${customerLabel}</strong></div>
    <div><span>Item</span><strong>${selected.length} item</strong></div>
    ${mode === "draft" ? "" : `<div><span>Pembayaran</span><strong>${selectedPayment}</strong></div>`}
    ${rewardAmount ? `<div><span>Gratis Member</span><strong>${reward.serviceName} · - ${formatMoney(rewardAmount)}</strong></div>` : ""}
    ${dp ? `<div><span>DP</span><strong>- ${formatMoney(dp)}</strong></div>` : ""}
    <div><span>Total</span><strong>${formatMoney(payable)}</strong></div>
  `;
  modal.hidden = false;
}

function closeConfirmation() {
  document.querySelector("#confirm-modal").hidden = true;
}

function openAddCustomerModal() {
  const modal = document.querySelector("#add-customer-modal");
  if (modal) modal.hidden = false;
}

function closeAddCustomerModal() {
  const modal = document.querySelector("#add-customer-modal");
  if (modal) modal.hidden = true;
}

function showToast(message) {
  const toast = document.querySelector("#toast");
  toast.textContent = message;
  toast.hidden = false;
  window.clearTimeout(showToast.timeout);
  showToast.timeout = window.setTimeout(() => {
    toast.hidden = true;
  }, 1800);
}

function setView(id) {
  document.querySelectorAll(".view").forEach((view) => {
    view.classList.toggle("active", view.id === id);
  });

  if (id === "customer-list-view") {
    renderCustomerList();
    const customerList = document.querySelector("#customer-list");
    if (customerList) customerList.scrollTop = 0;
  }
  if (id === "customer-view") renderCustomerDetail(activeDetailCustomerId);
}

document.addEventListener("click", (event) => {
  const detailCustomer = event.target.closest("[data-detail-customer]");
  if (detailCustomer) {
    activeDetailCustomerId = detailCustomer.dataset.detailCustomer;
    renderCustomerDetail(activeDetailCustomerId);
    setView("customer-view");
    return;
  }

  if (event.target.closest("#add-customer-button")) {
    openAddCustomerModal();
    return;
  }

  if (event.target.closest("#cancel-add-customer")) {
    closeAddCustomerModal();
    return;
  }

  if (event.target.closest("#save-add-customer")) {
    closeAddCustomerModal();
    showToast("Contoh pelanggan baru tersimpan");
    return;
  }

  if (event.target.id === "add-customer-modal") {
    closeAddCustomerModal();
    return;
  }

  const customerToggle = event.target.closest("#customer-toggle");
  if (customerToggle) {
    const picker = document.querySelector("#customer-picker");
    picker.classList.toggle("open");
    renderCustomerDropdown();
    return;
  }

  const customerOption = event.target.closest("[data-customer]");
  if (customerOption) {
    selectedCustomer = customers.find((customer) => customer.id === customerOption.dataset.customer);
    document.querySelector("#customer-picker").classList.remove("open");
    renderCustomer();
    renderCustomerDropdown();
    renderCart();
    return;
  }

  const staffButton = event.target.closest("[data-staff-for]");
  if (staffButton) {
    activeStaffMenu = activeStaffMenu === staffButton.dataset.staffFor ? null : staffButton.dataset.staffFor;
    renderCart();
    return;
  }

  const staffChoice = event.target.closest("[data-staff]");
  if (staffChoice) {
    const item = items.find((entry) => entry.id === staffChoice.dataset.id);
    item.staff = staffChoice.dataset.staff;
    activeStaffMenu = null;
    renderCart();
    return;
  }

  const paymentButton = event.target.closest("[data-payment]");
  if (paymentButton) {
    setPayment(paymentButton.dataset.payment);
    return;
  }

  const payButton = event.target.closest("#pay-button");
  if (payButton) {
    const hasItems = items.some((item) => item.qty > 0);
    if (!hasItems) {
      showToast("Keranjang masih kosong");
      return;
    }
    openConfirmation("payment");
    return;
  }

  const draftButton = event.target.closest("#draft-button");
  if (draftButton) {
    const hasItems = items.some((item) => item.qty > 0);
    if (!hasItems) {
      showToast("Keranjang masih kosong");
      return;
    }
    openConfirmation("draft");
    return;
  }

  if (event.target.closest("#cancel-payment")) {
    closeConfirmation();
    return;
  }

  if (event.target.closest("#confirm-payment")) {
    closeConfirmation();
    showToast(activeConfirmMode === "draft" ? "Transaksi masuk draft" : "Pembayaran tersimpan");
    return;
  }

  if (event.target.id === "confirm-modal") {
    closeConfirmation();
    return;
  }

  const nav = event.target.closest("[data-target]");
  if (nav) {
    setView(nav.dataset.target);
    return;
  }

  const filterButton = event.target.closest("[data-filter]");
  if (filterButton) {
    activeFilter = filterButton.dataset.filter;
    searchTerm = "";
    activeStaffMenu = null;
    document.querySelector("#item-search").value = "";
    document.querySelectorAll("[data-filter]").forEach((button) => {
      button.classList.toggle("active", button.dataset.filter === activeFilter);
    });
    updateSearchPlaceholder();
    renderItems();
    return;
  }

  const itemCard = event.target.closest(".item-card");
  if (itemCard) {
    const item = items.find((entry) => entry.id === itemCard.dataset.id);
    const changed = increaseItem(item);
    activeStaffMenu = null;
    renderItems();
    renderCart();
    if (!changed) return;
    return;
  }

  const quantityButton = event.target.closest(".cart-qty button");
  if (quantityButton) {
    const quantity = quantityButton.closest(".cart-qty");
    const item = items.find((entry) => entry.id === quantity.dataset.id);
    const delta = Number(quantityButton.dataset.delta);
    if (delta > 0) {
      increaseItem(item);
    } else {
      item.qty = Math.max(0, item.qty + delta);
    }
    activeStaffMenu = null;
    renderCart();
    return;
  }

  if (activeStaffMenu && !event.target.closest(".staff-menu") && !event.target.closest("[data-staff-for]")) {
    activeStaffMenu = null;
    renderCart();
  }

  if (!event.target.closest("#customer-picker")) {
    document.querySelector("#customer-picker").classList.remove("open");
  }
});

document.addEventListener("input", (event) => {
  const customerSearch = event.target.closest("#customer-search");
  if (customerSearch) {
    customerSearchTerm = customerSearch.value.trim().toLowerCase();
    renderCustomerList();
    return;
  }

  const searchInput = event.target.closest("#item-search");
  if (!searchInput) return;

  searchTerm = searchInput.value.trim().toLowerCase();
  if (activeStaffMenu) {
    activeStaffMenu = null;
    renderCart();
  }
  renderItems();
});

renderCustomer();
renderCustomerDropdown();
updateSearchPlaceholder();
renderItems();
renderCart();
renderCustomerList();
renderCustomerDetail(activeDetailCustomerId);
setPayment(selectedPayment);
